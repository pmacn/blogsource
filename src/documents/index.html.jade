---
layout: 'default'
title: 'Home'
referencesOthers: true
---

for post in getCollection('posts').toJSON()
    a(href!= post.url)
        h2!= post.title
    if hasIngress(post)
        p!= getIngress(post)

    p.text-right
        a(href!= post.url) read more